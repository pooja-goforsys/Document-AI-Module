fastapi==0.115.4
uvicorn[standard]==0.32.1
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
pgvector==0.3.6
alembic==1.14.0
psycopg2-binary==2.9.10
python-multipart==0.0.12
python-dotenv==1.0.1
pydantic==2.10.3
pydantic-settings==2.6.1
sse-starlette==2.1.3

# Document parsers
pypdf==5.1.0
PyMuPDF>=1.24.0
pdfplumber>=0.11.4
python-docx==1.1.2
openpyxl==3.1.5

# Embeddings + reranking
# sentence-transformers provides both bi-encoder embedding and cross-encoder
# reranking.  The default reranker model (BAAI/bge-reranker-v2-m3) is loaded
# automatically on first use — no extra package required.
sentence-transformers==3.3.1

# Optional: Cohere Rerank API (set COHERE_API_KEY + RERANKER_BACKEND=cohere in .env)
# pip install cohere
# cohere>=5.0.0

# AI providers
google-genai>=2.0.0
openai==1.57.2
anthropic>=0.40.0

# Text splitting
langchain-text-splitters==0.3.2
tiktoken>=0.7.0

# Async file I/O
aiofiles==24.1.0
httpx==0.28.0

# Auth
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
python-jose[cryptography]==3.3.0
email-validator==2.2.0
